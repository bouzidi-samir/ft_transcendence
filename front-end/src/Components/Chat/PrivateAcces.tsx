import { Room } from "../../Slices/RoomSlice"
import "../../styles/Components/Chat/PrivateAccess.css"


export default function PrivateAcces({props} : any) {
    const {room, setPrivate } = props;

    return (
        <form className="privateAccess">

        </form>
    )
}